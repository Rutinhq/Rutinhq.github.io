name = "rutinhq-web"
compatibility_date = "2026-08-30"
pages_build_output_dir = "dist"

# RutinHQ Guide — Pages Function at functions/api/guide.ts
# Dashboard secrets (Production + Preview):
#   GUIDE_LLM_API_KEY
#   GUIDE_LEAD_WEBHOOK_URL
# Persist as Pages *secrets* (not dashboard plaintext, not [vars]):
#   GUIDE_LLM_API_KEY
#   GUIDE_LLM_BASE_URL = https://generativelanguage.googleapis.com/v1beta/openai
#   GUIDE_LLM_MODEL = gemini-2.0-flash
# Direct Upload wipes plaintext dashboard vars. The same name cannot be both
# a secret and a wrangler.toml [vars] binding ("already in use").
# Successful completions return mode=llm. See GUIDE.md.
